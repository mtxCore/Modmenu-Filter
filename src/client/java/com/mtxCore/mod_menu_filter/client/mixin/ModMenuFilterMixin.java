package com.mtxCore.mod_menu_filter.client.mixin;

/**
 * @deprecated No longer used.  Mod hiding is now handled natively by writing
 * to Mod Menu's {@code hidden_mods} config via {@code ModMenuConfigWriter}.
 * See {@link ModBadgeMixin} for tag-badge rendering.
 */
@Deprecated
public class ModMenuFilterMixin {
    // Intentionally empty — this class is kept only so the file compiles.
}